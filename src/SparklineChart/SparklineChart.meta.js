import SparklineChart from './SparklineChart';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SparklineChart);

metadata.exportInfo = {
  path: 'src/SparklineChart/SparklineChart.js',
};

import AreaChart from './AreaChart';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(AreaChart);

metadata.exportInfo = {
  path: 'src/AreaChart/AreaChart.js',
};

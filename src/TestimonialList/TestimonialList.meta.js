import TestimonialList from './TestimonialList';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TestimonialList);

metadata.exportInfo = {
  path: 'src/TestimonialList/TestimonialList.js',
};

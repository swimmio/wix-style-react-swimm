import SelectableAccordion from './SelectableAccordion';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SelectableAccordion);

metadata.exportInfo = {
  path: 'src/SelectableAccordion/SelectableAccordion.js',
};

import FeatureList from './FeatureList';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(FeatureList);

metadata.exportInfo = {
  path: 'src/FeatureList/FeatureList.js',
};

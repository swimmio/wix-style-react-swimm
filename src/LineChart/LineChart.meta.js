import LineChart from './LineChart';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(LineChart);

metadata.exportInfo = {
  path: 'src/LineChart/LineChart.js',
};

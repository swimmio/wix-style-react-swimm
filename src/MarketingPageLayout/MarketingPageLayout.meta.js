import MarketingPageLayout from './MarketingPageLayout';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(MarketingPageLayout);

metadata.exportInfo = {
  path: 'src/MarketingPageLayout/MarketingPageLayout.js',
};

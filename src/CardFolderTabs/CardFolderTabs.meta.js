import CardFolderTabs from './CardFolderTabs';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CardFolderTabs);

metadata.exportInfo = {
  path: 'src/CardFolderTabs/CardFolderTabs.js',
};
